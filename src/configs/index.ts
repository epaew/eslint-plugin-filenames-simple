import { all } from './all';
import { recommended, recommendedReact, recommendedVue } from './recommended';

export const configs = {
  all,
  recommended,
  'recommended-react': recommendedReact,
  'recommended-vue': recommendedVue,
};
